Q. What is the $_SERVER["PHP_SELF"] variable?
>> The $_SERVER["PHP_SELF"] is a super global variable that returns the filename of the currently executing script.

Q. What is the htmlspecialchars() function?
>> The htmlspecialchars() function converts special characters into HTML entities. This means that it will convert HTML characters like < and > with &lt; and &gt;. This prevents attackers from exploiting the code by injecting HTML or Javascript code (Cross-site Scripting attacks) in forms.

Q. Difference between GET and POST.
>> GET sends data via URL query string, while POST sends data in the request body.
   GET is used for non-sensitive, read-only operations like search or filtering.
   POST is used for submitting forms, sensitive data or modifying server state.

Q. Why is server-side validation required?
>> Client-side validation can be bypassed, so server-side validation is required to ensure data integrity and security.